% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/foreco-class.R
\name{foreco-class}
\alias{foreco-class}
\alias{foreco}
\alias{summary.foreco}
\alias{print.foreco}
\alias{print.summary_foreco}
\alias{plot.foreco}
\alias{components.foreco}
\alias{drop_foreco_class}
\alias{new_foreco_class}
\title{FoReco Reconciliation Class}
\usage{
new_foreco_class(reco, framework, rfun, rtype, rinfo = NULL, nninfo = NULL)

\method{summary}{foreco}(object, keep_forecasts = TRUE, ...)

\method{print}{summary_foreco}(x, n_row = 4L, n_col = 6L, ...)

\method{print}{foreco}(x, n_row = NULL, n_col = NULL, ...)

\method{plot}{foreco}(x, cs = NULL, te = 1, alpha = 0.95, ...)

\method{components}{foreco}(
  object,
  cs = NULL,
  te = NULL,
  keep_names = FALSE,
  temporal_names = NULL,
  simplify = FALSE,
  ...
)

drop_foreco_class(x)
}
\arguments{
\item{reco}{A numeric matrix/vector (when \code{rtype = "point"}) or a
distributional object (when \code{rtype = "probabilistic"}).}

\item{framework}{A character string identifying the reconciliation framework.
Must be one of \code{"cross-sectional"}, \code{"temporal"}, or \code{"cross-temporal"}.}

\item{rfun}{A character scalar with the name of the FoReco function that
produced the reconciled forecasts (e.g. \code{"csrec"}, \code{"terec"}, \code{"ctrec"}).}

\item{rtype}{A character string indicating the type of reconciled forecasts.
Must be one of \code{"point"} or \code{"probabilistic"}.}

\item{rinfo}{An optional named list with additional reconciliation
information (e.g. covariance approximation \code{comb}, machine-learning
approach \code{ml}, non-negativity flag \code{nn}, cross-sectional size \code{cs_n},
temporal aggregation set \code{te_set}, forecast horizon \code{forecast_horizon},
level-conditional reconciled forecasts \code{lcc}).}

\item{nninfo}{An optional matrix with information about the non-negativity
procedure applied during reconciliation. Stored as the \code{info} field of
the \code{"FoReco"} attribute.}

\item{keep_forecasts}{Logical; if \code{TRUE} (the default), the reconciled
forecasts are stored in the \code{rf} element of the returned
\code{summary_foreco} object (and therefore printed at the end of the summary).
Set to \code{FALSE} to obtain a lighter summary object that omits them.}

\item{...}{Additional arguments passed to the underlying methods
(e.g. \code{print()}).}

\item{x, object}{An object of class \code{foreco}.}

\item{n_row, n_col}{Integers giving the maximum number of rows and columns to
display when printing. If \code{NULL} (the default) all rows/columns are shown.
When the matrix is truncated, a summary line reports how many rows and
columns have been omitted.}

\item{cs}{Optional integer vector selecting the cross-sectional series to
keep. If \code{NULL} (the default) all series are returned.}

\item{te}{Optional vector (numeric or character) selecting the temporal
aggregation orders to keep, matched against the elements of \code{te_set}. If
\code{NULL} (the default) all orders are returned.}

\item{alpha}{Nominal coverage of the prediction interval drawn by
\code{plot.foreco()} for probabilistic forecasts. Defaults to \code{0.95}.}

\item{keep_names}{Logical. If \code{TRUE}, the row/column names of the reconciled
forecasts are preserved in the output of \code{components.foreco()}. Defaults
to \code{FALSE}.}

\item{temporal_names}{Optional character vector of labels for the temporal
aggregation orders returned by \code{components.foreco()}. Its length must
match the number of returned orders, otherwise a warning is emitted and
the default \code{"k-..."} labels are used.}

\item{simplify}{Logical. If \code{TRUE} and the result consists of a single
component, the underlying object (a matrix or vector) is returned directly
instead of being wrapped in a named list of length one. If \code{FALSE}
(default), the output is always a named list.}
}
\value{
A \code{foreco} object extending the reconciled forecasts/distributions with
reconciliation metadata.

\code{components.foreco()} returns a named list of reconciled forecasts split
by temporal aggregation order. For the cross-sectional framework the list
has a single element \code{"k-1"}.
}
\description{
The \code{foreco} class represents reconciled forecasts produced by the FoReco
package. It extends a numeric matrix, vector, or distributional object with
additional attributes that store metadata about the reconciliation procedure
(framework, function used, forecast type, and other reconciliation-specific
information). The class provides dedicated methods for printing,
summarising, extracting components and visualising reconciled forecasts.
}
\details{
\code{new_foreco_class()} is the low-level constructor. It is exported so
that companion packages can produce objects that integrate with FoReco's
\code{print()}, \code{summary()}, \code{plot()} and \code{components()} methods.

\code{plot.foreco()} draws the reconciled forecasts as line/point plots. For
probabilistic forecasts (\code{rtype = "probabilistic"}) it also overlays a
shaded central \verb{alpha * 100\%} prediction interval, built from the
\code{(1 - alpha)/2} and \code{1 - (1 - alpha)/2} quantiles of the distributional
object; the median is shown as a dashed line and the interval limits as
dotted lines.
}
\examples{
set.seed(123)
# Aggregation matrix for Z = X + Y
A   <- t(c(1, 1))
bts <- matrix(rnorm(6, mean = 10), 3, 2)
reco <- csbu(base = bts, agg_mat = A)

# Print and summarise the reconciled forecasts
print(reco)
print(reco, n_row = 2, n_col = 2)
summary(reco)
summary(reco, keep_forecasts = FALSE)

# Extract reconciled forecasts by temporal aggregation order
components(reco)

# Remove the foreco class
drop_foreco_class(reco)

}
